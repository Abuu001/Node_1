/**
 * Performs addition
 * @param {Number} a  Should be an integer
 * @param {Number} b  Should be an intager
 * @returns {Number}   End result should be asumation of a and b
 * 
 * Example
 * add(2,4)
 * returns 6
 */
const add = (a,b)=>{
    return a+b;
}
 
add(2,3)